{{/*
PVC is managed by the StatefulSet volumeClaimTemplate in postgres-statefulset.yaml.
This file is kept as a placeholder for documentation purposes.
If you need a standalone PVC (e.g., for migration), uncomment below:
*/}}
{{/*
{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mcp-hosting.fullname" . }}-postgres
  namespace: {{ include "mcp-hosting.namespace" . }}
  labels:
    {{- include "mcp-hosting.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.postgres.storageClass }}
  storageClassName: {{ .Values.postgres.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.postgres.storage }}
{{- end }}
*/}}
