# Environment files (contain secrets)
.env
.env.local
.env.production

# Terraform state and local config
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
*.tfvars
!*.tfvars.example

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker override (local dev)
docker-compose.override.yml

# Backup artifacts
*.sql
*.sql.gz

# test.sh local + CI artifacts — uploaded as GHA artifacts, never committed
test-results/

# AI-agent local state (Claude Code + similar tools)
.claude/
