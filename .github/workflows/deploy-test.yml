name: Deploy Tests

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
  workflow_dispatch:
    inputs:
      target:
        description: "Single target to run (docker-compose, helm, fly, cloudrun) or empty for all"
        default: ""
      image:
        description: "App image to test against"
        default: "ghcr.io/yawlabs/mcp-hosting:latest"

concurrency:
  group: deploy-test-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: read  # needed to pull the private ghcr.io/yawlabs/mcp-hosting image

jobs:
  test:
    name: ${{ matrix.target }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: [docker-compose, helm, fly, cloudrun]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install yamllint
        if: matrix.target == 'cloudrun'
        run: pip install yamllint

      - name: Set up Helm
        if: matrix.target == 'helm'
        uses: azure/setup-helm@v4

      # GHCR login for the docker-compose E2E which pulls the private
      # ghcr.io/yawlabs/mcp-hosting image. The GITHUB_TOKEN here works
      # only if the package has granted this repo read access. If this
      # step fails with "unauthorized", visit the package settings on
      # GitHub and add the repo under Manage Actions access.
      - name: Log in to GHCR
        if: matrix.target == 'docker-compose'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Run target
        run: bash test.sh --target=${{ matrix.target }}
        env:
          IMAGE: ${{ github.event.inputs.image || 'ghcr.io/yawlabs/mcp-hosting:latest' }}

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: results-${{ matrix.target }}
          path: test-results/
          if-no-files-found: ignore
